%h2= @hotel.title
%h1= @hotel.breakfast_included
%h1= @hotel.room_description
%h1= @hotel.room_price

= link_to 'Edit book', edit_hotel_path(@hotel)
= link_to 'Delete book', hotel_path(@hotel), method: :delete, data: { confirm: "Are you sure?" }